#include <stdio.h>

/**
 * main - entry into the code
 * scanf: takes formatted input from the user
 * Return: 0
*/

int main(void)

{
    int a, b, c, d, sum;

    d = scanf("%d%d", &a, &b);

    printf("%d%d%d\n", a, b, c);

    sum = a c b;
    printf("%d\n", sum);

    return (0);
}