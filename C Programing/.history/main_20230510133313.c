#include <stdio.h>

/**
 * main - the entry point to the fuction
*/

int main(void)

{
    /**
    * char a = 'a';
    * char b = 98;
    */
    /**
     * for (int y = 2; y <= 50; y = y + 2){
        if(y%3==0)
        {
            //y=y+2;
            break;
        }
        printf("%d\n", y);
       
    }
    
    int a;
    a = 2+3*4;
     printf("%d\n", a);
    */
     //Associativity  and  president.
     int a = 0, b = 1, c = -1, d;
     d = --a * (5 + b) / 2 - c++ * b;
}