#include <stdio.h>

/**
 * main - main to the code
 * n: int value
 * Return: 0
*/

int main(void){
    int total = 1;
    int n=5;

        for (int b = 1; b <= n; b++){

            total = total * b;
        }

        printf("%d\n", total);

        return(0);
}